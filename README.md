# Marais — E-Commerce Jewelry Store

## 📋 Состояние проекта

✅ **Завершено:**
- Django 6.0 настроен и работает
- Миграции БД приложены (users, main, basket, catalog, reviews)
- Статические файлы (CSS, изображения) сконфигурированы
- HTML структура переписана точно по макету дизайна
- CSS стили добавлены со всеми нужными правилами
- Адаптивный дизайн для мобильных, планшетов и десктопов
- SVG плейсхолдеры добавлены для всех изображений

## 🚀 Быстрый старт

### 1. Активировать виртуальное окружение
```bash
cd /Users/tab1k/Documents/ZIZ.INC/marais
source venv/bin/activate
```

### 2. Запустить сервер разработки
```bash
cd src
python manage.py runserver 0.0.0.0:8000
```

### 3. Открыть в браузере
```
http://localhost:8000/
```

## 📁 Структура проекта

```
marais/
├── venv/                          # Виртуальное окружение Python
├── src/                           # Django проект
│   ├── manage.py                  # Django управления
│   ├── db.sqlite3                 # База данных (development)
│   ├── marais/                    # Основные настройки
│   │   ├── settings.py            # Конфигурация Django
│   │   ├── urls.py                # URL маршруты проекта
│   │   ├── wsgi.py                # WSGI для продакшена
│   │   └── asgi.py                # ASGI для async
│   ├── users/                     # Приложение пользователей
│   │   ├── models.py              # CustomUser модель
│   │   ├── views.py               # Представления
│   │   ├── admin.py               # Админ панель
│   │   ├── urls.py                # URL маршруты приложения
│   │   └── migrations/            # Миграции БД
│   ├── main/                      # Главное приложение
│   │   ├── views.py               # Главные представления
│   │   ├── models.py              # Модели
│   │   └── urls.py                # URL маршруты
│   ├── basket/                    # Приложение корзины
│   │   └── ...
│   ├── catalog/                   # Приложение каталога
│   │   └── ...
│   ├── reviews/                   # Приложение отзывов
│   │   └── ...
│   ├── templates/                 # HTML шаблоны
│   │   ├── base.html              # Базовый шаблон
│   │   ├── main/
│   │   │   └── general.html       # Главная страница ✨ (ПЕРЕПИСАНА)
│   │   └── includes/
│   │       ├── header.html        # Шапка сайта
│   │       └── footer.html        # Футер сайта
│   └── static/                    # Статические файлы
│       ├── css/
│       │   └── style.css          # Главные стили ✨ (ПЕРЕПИСАНЫ)
│       └── images/
│           ├── logo.svg           # Логотип
│           ├── hero-placeholder.svg
│           ├── product-placeholder.svg
│           └── thumb-placeholder.svg
├── ASSETS_GUIDE.md                # 📖 Руководство по добавлению изображений
└── README.md                      # Этот файл
```

## 🎨 Что было переделано

### ✨ HTML структура (`templates/main/general.html`)
Полностью переписана для соответствия дизайну Figma:

1. **Hero-секция** — Центральное изображение + текст "MARAIS"
2. **Категории** — 4 квадратные карточки (Браслеты, Серьги, Ожерелья, Кольца)
3. **Коллекции** — 3 альтернирующих блока с изображениями и текстом
4. **Товары** — 8 карточек в сетке (4 колонки на десктопе, 2 на мобильных)
5. **Отзывы** — 4 карточки с оценками (звезды)
6. **CTA** — Блок "Готовы начать покупки?" с кнопкой

### 🎨 CSS стили (`static/css/style.css`)
Полностью переписаны с:

- **Цветовая палитра**: 
  - Gold `#c9a35e` — основной акцент
  - Red `#a71930` — кнопки и footer
  - Light bg `#f9f7f4` — фоны секций
  
- **Типография**: 
  - Georgia serif для заголовков
  - Segoe UI для текста
  - Правильное выравнивание и отступы

- **Компоненты**:
  - `.hero-section` — главная секция
  - `.category-card` — карточки категорий
  - `.product-card` — карточки товаров
  - `.testimonial-card` — карточки отзывов
  - `.btn-*` — различные кнопки

- **Адаптивность**:
  - Desktop: 1200px+ (полная ширина)
  - Tablet: 768px-991px (2 колонны)
  - Mobile: <768px (1 колонна)

## 🖼️ Как добавить свои изображения

### Быстрая инструкция:
1. Экспортируйте изображения из Figma в PNG или WebP
2. Замените файлы в `src/static/images/`
3. Обновите HTML в `templates/main/general.html` если нужны разные файлы для разных изображений

### Детальное руководство:
Смотрите файл **`ASSETS_GUIDE.md`** в корне проекта.

## 🗄️ База данных

### Текущие модели:
- **CustomUser** (users app) — расширенная модель пользователя
- **Main app** — основное приложение (пусто, готово для расширения)
- **Basket** — приложение корзины (пусто)
- **Catalog** — приложение каталога (пусто)
- **Reviews** — приложение отзывов (пусто)

### Запуск миграций:
```bash
cd src
python manage.py migrate
```

### Создание суперпользователя (админ):
```bash
python manage.py createsuperuser
# Введите username, email, пароль
```

### Доступ в админ-панель:
```
http://localhost:8000/admin/
```

## 📱 Адаптивные точки разрыва (Breakpoints)

Сайт работает хорошо на:
- 🖥️ **Desktop** 1440px и выше — полная версия
- 📱 **Tablet** 768px-1199px — оптимизированная версия
- 📱 **Mobile** до 768px — мобильная версия

Все сетки автоматически переходят на нужное количество колонн.

## 🔧 Технологический стек

| Компонент | Версия | Описание |
|-----------|--------|----------|
| Django | 6.0 | Web-фреймворк |
| Python | 3.12 | Язык программирования |
| Bootstrap | 5.3.2 | CSS фреймворк (CDN) |
| SQLite3 | Latest | База данных (dev) |

## 📝 Конфигурация

### `settings.py` ключевые параметры:
```python
AUTH_USER_MODEL = 'users.CustomUser'  # Пользовательская модель пользователя
STATIC_URL = '/static/'               # URL для статических файлов
STATICFILES_DIRS = [BASE_DIR / 'static']  # Папки для поиска статики
LANGUAGE_CODE = 'ru-ru'               # Русский язык
```

## 🚢 Развертывание (Production)

Для развертывания на продакшене:

1. **Установить gunicorn**:
```bash
pip install gunicorn
```

2. **Собрать статические файлы**:
```bash
python manage.py collectstatic
```

3. **Запустить с gunicorn**:
```bash
gunicorn marais.wsgi:application --bind 0.0.0.0:8000
```

4. **Использовать Nginx** как reverse proxy

## 📝 Дальнейшие шаги

### 1. Добавить реальные данные
- Создать товары через админ-панель
- Добавить категории
- Наполнить отзывы

### 2. Интегрировать с БД
- Обновить шаблон, чтобы загружал товары из БД
- Создать страницы товаров
- Добавить фильтрацию и поиск

### 3. Функциональность
- Реализовать корзину покупок
- Добавить систему отзывов
- Интегрировать платежи (Stripe, Yandex.Kassa и т.д.)

### 4. SEO и оптимизация
- Добавить мета-теги
- Оптимизировать изображения
- Настроить кэширование

## ❓ Часто задаваемые вопросы

**Q: Как изменить текст на странице?**
A: Отредактируйте `templates/main/general.html` и найдите нужный текст.

**Q: Как добавить новую секцию?**
A: Скопируйте одну из существующих секций в `general.html`, отредактируйте класс CSS в `style.css`.

**Q: Как подключить БД товаров?**
A: Создайте модель в `catalog/models.py`, затем обновите шаблон чтобы использовать `{% for product in products %}`.

**Q: Почему изображения не загружаются?**
A: Убедитесь, что:
- Файлы лежат в `src/static/images/`
- Вы используете `{% load static %}`
- В шаблоне `{% static 'images/filename.svg' %}`

## 📞 Контакты разработчика

Проект создан для Marais — jewelry e-commerce store

---

**Версия**: 1.0.0  
**Дата**: 7 декабря 2025  
**Статус**: ✅ Готово к использованию
